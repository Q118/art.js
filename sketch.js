/** @format */

function setup() {
	createCanvas(400, 400);
}
function draw() {
	background(120);
 }
// We create the canvas on the method setup() because it runs only once the program gets loaded the first time. The method draw() keeps running till the program ends, and there we define the code wanted to render on the screen. 